const familyMap = new Map([

  ["RHEIDAE", {
    text: "RHEIDAE (RHEAS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline.htm"
  }],
  ["TINAMIDAE", {
    text: "TINAMIDAE (TINAMOUS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline.htm"
  }],
  ["ANHIMIDAE", {
    text: "ANHIMIDAE (SCREAMERS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline.htm"
  }],
  ["ANATIDAE", {
    text: "ANATIDAE (DUCKS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline.htm"
  }],
  ["CRACIDAE", {
    text: "CRACIDAE (GUANS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline.htm"
  }],
  ["ODONTOPHORIDAE", {
    text: "ODONTOPHORIDAE (NEW WORLD QUAILS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline.htm"
  }],
  ["PHASIANIDAE", {
    text: "PHASIANIDAE (PHEASANTS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline.htm"
  }],
  ["PHOENICOPTERIDAE", {
    text: "PHOENICOPTERIDAE (FLAMINGOS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline.htm"
  }],
  ["PODICIPEDIDAE", {
    text: "PODICIPEDIDAE (GREBES)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline.htm"
  }],

  ["COLUMBIDAE", {
    text: "COLUMBIDAE (PIGEONS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline02.htm"
  }],
  ["CUCULIDAE", {
    text: "CUCULIDAE (CUCKOOS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline02.htm"
  }],
  ["STEATORNITHIDAE", {
    text: "STEATORNITHIDAE (OILBIRD)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline02.htm"
  }],
  ["NYCTIBIIDAE", {
    text: "NYCTIBIIDAE (POTOOS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline02.htm"
  }],
  ["CAPRIMULGIDAE", {
    text: "CAPRIMULGIDAE (NIGHTJARS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline02.htm"
  }],

  ["APODIDAE", {
    text: "APODIDAE (SWIFTS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline03.htm"
  }],
  ["TROCHILIDAE", {
    text: "TROCHILIDAE (HUMMINGBIRDS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline03.htm"
  }],

  ["OPISTHOCOMIDAE", {
    text: "OPISTHOCOMIDAE (HOATZIN)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["ARAMIDAE", {
    text: "ARAMIDAE (LIMPKIN)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["PSOPHIIDAE", {
    text: "PSOPHIIDAE (TRUMPETERS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["RALLIDAE", {
    text: "RALLIDAE (RAILS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["HELIORNITHIDAE", {
    text: "HELIORNITHIDAE (FINFOOTS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["CHARADRIIDAE", {
    text: "CHARADRIIDAE (PLOVERS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["HAEMATOPODIDAE", {
    text: "HAEMATOPODIDAE (OYSTERCATCHERS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["RECURVIROSTRIDAE", {
    text: "RECURVIROSTRIDAE (AVOCETS and STILTS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["BURHINIDAE", {
    text: "BURHINIDAE (THICK-KNEES)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["CHIONIDAE", {
    text: "CHIONIDAE (SHEATHBILLS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["PLUVIANELLIDAE", {
    text: "PLUVIANELLIDAE (MAGELLANIC PLOVER)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["SCOLOPACIDAE", {
    text: "SCOLOPACIDAE (SANDPIPERS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["THINOCORIDAE", {
    text: "THINOCORIDAE (SEEDSNIPES)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["JACANIDAE", {
    text: "JACANIDAE (JACANAS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["ROSTRATULIDAE", {
    text: "ROSTRATULIDAE (PAINTED-SNIPES)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["GLAREOLIDAE", {
    text: "GLAREOLIDAE (PRATINCOLES)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["STERCORARIIDAE", {
    text: "STERCORARIIDAE (SKUAS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["LARIDAE", {
    text: "LARIDAE (GULLS and TERNS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["EURYPYGIDAE", {
    text: "EURYPYGIDAE (SUNBITTERN)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["PHAETHONTIDAE", {
    text: "PHAETHONTIDAE (TROPICBIRDS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["SPHENISCIDAE", {
    text: "SPHENISCIDAE (PENGUINS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["DIOMEDEIDAE", {
    text: "DIOMEDEIDAE (ALBATROSSES)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["OCEANITIDAE", {
    text: "OCEANITIDAE (SOUTHERN STORM-PETRELS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["HYDROBATIDAE", {
    text: "HYDROBATIDAE (NORTHERN STORM-PETRELS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["PROCELLARIIDAE", {
    text: "PROCELLARIIDAE (SHEARWATERS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["CICONIIDAE", {
    text: "CICONIIDAE (STORKS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["FREGATIDAE", {
    text: "FREGATIDAE (FRIGATEBIRDS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["SULIDAE", {
    text: "SULIDAE (BOOBIES)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["ANHINGIDAE", {
    text: "ANHINGIDAE (ANHINGAS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["PHALACROCORACIDAE", {
    text: "PHALACROCORACIDAE (CORMORANTS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["PELECANIDAE", {
    text: "PELECANIDAE (PELICANS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["ARDEIDAE", {
    text: "ARDEIDAE (HERONS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["THRESKIORNITHIDAE", {
    text: "THRESKIORNITHIDAE (IBISES)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["CATHARTIDAE", {
    text: "CATHARTIDAE (NEW WORLD VULTURES)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["PANDIONIDAE", {
    text: "PANDIONIDAE (OSPREY)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["ACCIPITRIDAE", {
    text: "ACCIPITRIDAE (HAWKS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["TYTONIDAE", {
    text: "TYTONIDAE (BARN OWLS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],
  ["STRIGIDAE", {
    text: "STRIGIDAE (OWLS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline04.htm"
  }],

  ["TROGONIDAE", {
    text: "TROGONIDAE (TROGONS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline05.htm"
  }],
  ["MOMOTIDAE", {
    text: "MOMOTIDAE (MOTMOTS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline05.htm"
  }],
  ["ALCEDINIDAE", {
    text: "ALCEDINIDAE (KINGFISHERS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline05.htm"
  }],
  ["GALBULIDAE", {
    text: "GALBULIDAE (JACAMARS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline05.htm"
  }],
  ["BUCCONIDAE", {
    text: "BUCCONIDAE (PUFFBIRDS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline05.htm"
  }],
  ["CAPITONIDAE", {
    text: "CAPITONIDAE (NEW WORLD BARBETS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline05.htm"
  }],
  ["SEMNORNITHIDAE", {
    text: "SEMNORNITHIDAE (TOUCAN-BARBETS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline05.htm"
  }],
  ["RAMPHASTIDAE", {
    text: "RAMPHASTIDAE (TOUCANS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline05.htm"
  }],
  ["PICIDAE", {
    text: "PICIDAE (WOODPECKERS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline05.htm"
  }],
  ["CARIAMIDAE", {
    text: "CARIAMIDAE (SERIEMAS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline05.htm"
  }],
  ["FALCONIDAE", {
    text: "FALCONIDAE (FALCONS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline05.htm"
  }],
  ["PSITTACULIDAE", {
    text: "PSITTACULIDAE (OLD WORLD PARROTS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline05.htm"
  }],
  ["PSITTACIDAE", {
    text: "PSITTACIDAE (NEW WORLD and AFRICAN PARROTS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline05.htm"
  }],

  ["SAPAYOIDAE", {
    text: "SAPAYOIDAE (SAPAYOA)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline06.htm"
  }],
  ["THAMNOPHILIDAE", {
    text: "THAMNOPHILIDAE (ANTBIRDS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline06.htm"
  }],
  ["MELANOPAREIIDAE", {
    text: "MELANOPAREIIDAE (CRESCENTCHESTS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline06.htm"
  }],
  ["CONOPOPHAGIDAE", {
    text: "CONOPOPHAGIDAE (GNATEATERS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline06.htm"
  }],
  ["GRALLARIIDAE", {
    text: "GRALLARIIDAE (ANTPITTAS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline06.htm"
  }],
  ["RHINOCRYPTIDAE", {
    text: "RHINOCRYPTIDAE (TAPACULOS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline06.htm"
  }],
  ["FORMICARIIDAE", {
    text: "FORMICARIIDAE (ANTTHRUSHES)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline06.htm"
  }],

  ["FURNARIIDAE", {
    text: "FURNARIIDAE (OVENBIRDS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline07.htm"
  }],

  ["PIPRIDAE", {
    text: "PIPRIDAE (MANAKINS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline08.htm"
  }],
  ["COTINGIDAE", {
    text: "COTINGIDAE (COTINGAS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline08.htm"
  }],
  ["TITYRIDAE", {
    text: "TITYRIDAE (TITYRAS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline08.htm"
  }],
  ["OXYRUNCIDAE", {
    text: "OXYRUNCIDAE (SHARPBILL)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline08.htm"
  }],
  ["ONYCHORHYNCHIDAE", {
    text: "ONYCHORHYNCHIDAE (ROYAL FLYCATCHERS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline08.htm"
  }],
  ["TYRANNIDAE", {
    text: "TYRANNIDAE (TYRANT FLYCATCHERS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline08.htm"
  }],

  ["VIREONIDAE", {
    text: "VIREONIDAE (VIREOS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline09.htm"
  }],
  ["CORVIDAE", {
    text: "CORVIDAE (JAYS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline09.htm"
  }],
  ["ALAUDIDAE", {
    text: "ALAUDIDAE (LARKS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline09.htm"
  }],
  ["HIRUNDINIDAE", {
    text: "HIRUNDINIDAE (SWALLOWS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline09.htm"
  }],
  ["TROGLODYTIDAE", {
    text: "TROGLODYTIDAE (WRENS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline09.htm"
  }],
  ["POLIOPTILIDAE", {
    text: "POLIOPTILIDAE (GNATCATCHERS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline09.htm"
  }],
  ["DONACOBIIDAE", {
    text: "DONACOBIIDAE (DONACOBIUS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline09.htm"
  }],
  ["CINCLIDAE", {
    text: "CINCLIDAE (DIPPERS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline09.htm"
  }],
  ["BOMBYCILLIDAE", {
    text: "BOMBYCILLIDAE (WAXWINGS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline09.htm"
  }],
  ["TURDIDAE", {
    text: "TURDIDAE (THRUSHES)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline09.htm"
  }],
  ["MUSCICAPIDAE", {
    text: "MUSCICAPIDAE (OLD WORLD FLYCATCHERS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline09.htm"
  }],
  ["MIMIDAE", {
    text: "MIMIDAE (MOCKINGBIRDS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline09.htm"
  }],
  ["STURNIDAE", {
    text: "STURNIDAE (STARLINGS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline09.htm"
  }],

  ["PLOCEIDAE", {
    text: "PLOCEIDAE (WEAVERS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline010.htm"
  }],
  ["ESTRILDIDAE", {
    text: "ESTRILDIDAE (ESTRILDIDS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline010.htm"
  }],
  ["PASSERIDAE", {
    text: "PASSERIDAE (OLD WORLD SPARROWS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline010.htm"
  }],
  ["MOTACILLIDAE", {
    text: "MOTACILLIDAE (PIPITS and WAGTAILS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline010.htm"
  }],
  ["FRINGILLIDAE", {
    text: "FRINGILLIDAE (FINCHES)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline010.htm"
  }],
  ["RHODINOCICHLIDAE", {
    text: "RHODINOCICHLIDAE (THRUSH-TANAGER)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline010.htm"
  }],
  ["PASSERELLIDAE", {
    text: "PASSERELLIDAE (SPARROWS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline010.htm"
  }],

  ["ICTERIDAE", {
    text: "ICTERIDAE (BLACKBIRDS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline011.htm"
  }],
  ["PARULIDAE", {
    text: "PARULIDAE (WOOD-WARBLERS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline011.htm"
  }],
  ["MITROSPINGIDAE", {
    text: "MITROSPINGIDAE (MITROSPINGIDS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline011.htm"
  }],
  ["CARDINALIDAE", {
    text: "CARDINALIDAE (CARDINAL GROSBEAKS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline011.htm"
  }],
  ["THRAUPIDAE", {
    text: "THRAUPIDAE (TANAGERS)",
    url: "https://www.museum.lsu.edu/~Remsen/SACCBaseline011.htm"
  }]
]);



function navigateToFamily(familyKey) {

    const data = familyMap.get(familyKey.toUpperCase());
    
    if (!data) {
        console.error("Family not found");
        return;
    }

    
    const fragment = encodeURIComponent(data.text);
    const fullUrl = `${data.url}#:~:text=${fragment}`;

    
    const link = document.createElement('a');
    link.href = fullUrl;
    link.target = '_blank';
    link.rel = 'noopener noreferrer';

    
    
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link); 
}



















;


var sampleTable;
var leftCheck;
var sciNames;
var italics;
var lineNumbers;
var showEndemics;
var showEndemicBreeders;

var currentCountry;
var previousHighlightedCountryNode;  
var checklistAuthorsPanel;
var AuthorsAbridged;
var checklistFlyoutText;


var countryMenuLayer;
var countryButton;

var gNumDays;
var gStartDate;
var previousNumDaysClass;

var numDaysButton;
var pdfButton;
var csvButton;
var requested;




var activityData = [];

var countries2Postals = {   "Argentina": "AR", "Aruba": "AW", "Bolivia": "BO", "Brazil": "BR", "Chile": "CL",
  "Colombia": "CO", "Curaçao": "CW", "Ecuador": "EC", "French Guiana": "GF",
  "Guyana": "GY", "Paraguay": "PY", "Peru": "PE", "Suriname": "SU", "Trinidad": "TT",
  "Uruguay": "UY", "Venezuela": "VE", "Bonaire": "BQ", "Falklands": "FK",
  "South America": "SAM"
};




window.addEventListener("load", function () {

  countryButton = document.getElementById("countryButton");
  countryButton.addEventListener("click", toggleCountryMenuLayer);

  

  

  numDaysButton = document.getElementById("numDays");
  numDaysButton.addEventListener("click", setNumDays);
  
  numDaysButton.addEventListener('keyup', setNumDays);

  numDaysButton.children.item(9).classList.add("highlight");
  gNumDays = 8;

  sampleTable = document.getElementById("sampleTable");
  sampleTable.classList.add("numDays" + String(gNumDays));

  previousNumDaysClass = "numDays8";

  pdfButton = document.getElementById("pdfButton");
  pdfButton.addEventListener("click", openChecklistPage);

  csvButton = document.getElementById("csvButton");
  csvButton.addEventListener("click", getCSVText);

  lineNumbers = document.getElementById("lineNumbers");
  leftCheck = document.getElementById("leftCheck");
  showEndemics = document.getElementById("showEndemics");
  showEndemicBreeders = document.getElementById("showEndemicBreeders");

  sciNames = document.getElementById("sciNames");
  italics = document.getElementById("italics");

  lineNumbers.addEventListener("click", toggleSampleTableLineNumbers);
  leftCheck.addEventListener("click", toggleSampleTableLeftChecks);
  showEndemics.addEventListener("click", toggleSampleTableShowEndemics);
  showEndemicBreeders.addEventListener("click", toggleSampleTableShowEndemicBreeders);

  sciNames.addEventListener("click", toggleSampleTableSciNames);
  italics.addEventListener("click", toggleSampleTableItalics);

  lineNumbers.nextElementSibling.addEventListener("keyup", toggleSampleTableLineNumbers);
  leftCheck.nextElementSibling.addEventListener("keyup", toggleSampleTableLeftChecks);
  showEndemics.nextElementSibling.addEventListener("keyup", toggleSampleTableShowEndemics);
  showEndemicBreeders.nextElementSibling.addEventListener("keyup", toggleSampleTableShowEndemicBreeders);

  sciNames.nextElementSibling.addEventListener("keyup", toggleSampleTableSciNames);
  italics.nextElementSibling.addEventListener("keyup", toggleSampleTableItalics);

  checklistAuthorsPanel = document.getElementById("checklistAuthorsPanel");
  checklistFlyoutText = document.getElementById("checklistFlyoutText");

  document.querySelector("#country-menu").addEventListener("click", setCountry);
  document.querySelector("#country-menu").addEventListener("keyup", setCountry);

  leftCheck.checked = true;

  countryMenuLayer = document.getElementById("countryMenuLayer");

  getJSON("../Authors/AuthorsAbridged.json", data => AuthorsAbridged = data);

  

  initCurrentMap();

  
  
});














function toggleCountryMenuLayer(evt) {

  countryMenuLayer.classList.toggle("show");
  countryButton.classList.toggle("slideRight");

  if (evt) evt.stopPropagation();

  var list = document.querySelectorAll(".countryItem");

  if (countryMenuLayer.classList.contains("show")) {  
    list.forEach(function(element) {
      element.setAttribute("tabindex", "1");
    });
  }
  else {
    list.forEach(function(element) {
      element.setAttribute("tabindex", "-1");
    });
  }
}

function toggleSampleTableShowEndemics(evt) {

  
  if (evt.type === "keyup") {
    if (evt.keyCode !== 13) {
      return;
    }
    else {
      evt.target.previousElementSibling.checked = !evt.target.previousElementSibling.checked;
    }
  }
  sampleTable.querySelector("td.endemical").classList.toggle("showEndemics");
}

function toggleSampleTableShowEndemicBreeders(evt) {

  
  if (evt.type === "keyup") {
    if (evt.keyCode !== 13) {
      return;
    }
    else {
      evt.target.previousElementSibling.checked = !evt.target.previousElementSibling.checked;
    }
  }
  sampleTable.querySelector("td.endemicalBreeder").classList.toggle("showEndemicBreeders");
}

function toggleSampleTableSciNames(evt) {

  
  if (evt.type === "keyup") {
    if (evt.keyCode !== 13) {
      return;
    }
    else {
      evt.target.previousElementSibling.checked = !evt.target.previousElementSibling.checked;
    }
  }

  sampleTable.classList.toggle("noScientificNames");
  italics.disabled = !italics.disabled;
}

function toggleSampleTableItalics(evt) {

  
  if (evt.type === "keyup") {
    if (evt.keyCode !== 13) {
      return;
    }
    else {
      evt.target.previousElementSibling.checked = !evt.target.previousElementSibling.checked;
    }
  }

  sampleTable.classList.toggle("noItalics");
}

function toggleSampleTableLeftChecks(evt) {

  
  if (evt.type === "keyup") {
    if (evt.keyCode !== 13) {
      return;
    }
    else {
      evt.target.previousElementSibling.checked = !evt.target.previousElementSibling.checked;
    }
  }

  var list = sampleTable.querySelectorAll(".leftCheckBox, .familyHidden");

  Array.prototype.forEach.call(list, function (item) {
    item.classList.toggle("show");
  });
}

function toggleSampleTableLineNumbers(evt) {

  
  if (evt.type === "keyup") {
    if (evt.keyCode !== 13) {
      return;
    }
    else {
      evt.target.previousElementSibling.checked = !evt.target.previousElementSibling.checked;
    }
  }

  sampleTable.querySelector("td.lineNumbers").classList.toggle("showLineNumbers");
}

function setCountry(evt) {

  
  if (evt.type === "keyup" && evt.keyCode !== 13) {
    return;
  }

  if (!currentCountry) document.getElementById("tax-panel").classList.add("setTaxPanelHeight");

  if (previousHighlightedCountryNode) previousHighlightedCountryNode.classList.remove("highlight");
  evt.target.classList.add("highlight");
  previousHighlightedCountryNode = evt.target;

  toggleCountryMenuLayer();

  currentCountry = evt.target.innerText;
  updateActivityData("select");  

  

  

  if (currentCountry === "South America") countryButton.innerHTML = "SA";
  else countryButton.innerHTML = countries2Postals[currentCountry];

  setChecklistCountryAuthors(currentCountry);
  loadCountryTaxonomy(currentCountry);
}


function setChecklistCountryAuthors(country) {

  if (country === "Falklands")
    checklistFlyoutText.innerHTML = "Make a checklist for the " + country + " Islands";
  else checklistFlyoutText.innerHTML = "Make a checklist for " + country;

  if (country === "French Guiana") selectedCountryFill("FrenchGuiana");
  else if (country !== "South America") selectedCountryFill(country);
  else fillSAMmap("");  // when is this called ?

  // check because AuthorsAbridged hasn't been downloaded yet
  if (AuthorsAbridged) checklistAuthorsPanel.innerHTML = AuthorsAbridged[country];
  else checklistAuthorsPanel.innerHTML = 	"Remsen, et al. Country lists. &nbsp;6&nbsp;April&nbsp;2026. A <a href='citations.html' target='_blank'>classification</a> of the bird species of South America. American Ornithological Society.";
}

function setNumDays(evt) {

  // KeyboardEvent, type keyup, 13 === Enter
  if (evt.type === "keyup" && evt.keyCode !== 13) {
    return;
  }

  var day = evt.target;
  var list;   

  if (day.classList.contains("highlight")) {  
    return;
  }
  else if (gNumDays || gNumDays === 0) {
    numDaysButton.children[gNumDays + 1].classList.remove("highlight");
  }

  day.classList.add("highlight");
  gNumDays = parseInt(day.innerHTML);

  sampleTable.classList.remove(previousNumDaysClass);
  sampleTable.classList.add("numDays" + String(gNumDays));
  previousNumDaysClass = "numDays" + String(gNumDays);

  
  

  list = sampleTable.querySelectorAll("td.cds, th.cds");
  Array.prototype.forEach.call(list, function (item) {
    item.classList.remove("cds");
  });

  if (gNumDays === 6) {

    list = sampleTable.querySelectorAll("td:nth-child(6), th:nth-child(4)");
    Array.prototype.forEach.call(list, function (item) {
      item.classList.add("cds");
    });
  }
  else if (gNumDays === 7 || gNumDays === 8) {

    list = sampleTable.querySelectorAll("td:nth-child(7), th:nth-child(5)");
    Array.prototype.forEach.call(list, function (item) {
      item.classList.add("cds");
    });

  }
  else if (gNumDays === 9 || gNumDays === 10) {

    list = sampleTable.querySelectorAll("td:nth-child(8), th:nth-child(6)");
    Array.prototype.forEach.call(list, function (item) {
      item.classList.add("cds");
    });
  }
  else if (gNumDays === 11 || gNumDays === 12) {

    list = sampleTable.querySelectorAll("td:nth-child(8), td:nth-child(13), th:nth-child(6), th:nth-child(11)");
    Array.prototype.forEach.call(list, function (item) {
      item.classList.add("cds");
    });
  }
}

function getCSVText() {

  if (!currentCountry) return;

  requested = "csv";
  
  

  var tempCountry;

  if (currentCountry === "French Guiana") tempCountry = "FrenchGuiana";
  else if (currentCountry === "South America") tempCountry = "SouthAmerica";
  else if (currentCountry === "Curaçao") tempCountry = "Curacao";
  else tempCountry = currentCountry;

  if (tempCountry) {

    var form = document.createElement("form");
    form.setAttribute("method", "post");
    
    form.setAttribute("action", "./php/sendCSV.php?country=" + tempCountry + "");

    document.body.appendChild(form);

    form.submit();
    form.remove();
  }
}

function openChecklistPage() {

  if (!currentCountry) return;

  requested = "checklist";
  

  var vars;
  var tempCountry;

  if (currentCountry === "French Guiana") tempCountry = "FrenchGuiana";
  else if (currentCountry === "South America") tempCountry = "SouthAmerica";
  else if (currentCountry === "Curaçao") tempCountry = "Curacao";
  else tempCountry = currentCountry;

  if (gNumDays === undefined) gNumDays = 12;
  if (gStartDate === undefined) gStartDate = 1;

  vars = "?country=" + tempCountry;
  vars += "&num_days=" + gNumDays;
  vars += "&start_date=" + gStartDate;

  vars += "&line_nos=" + lineNumbers.checked;
  vars += "&left_check=" + leftCheck.checked;
  vars += "&endemics=" + showEndemics.checked;
  vars += "&endemicBreeders=" + showEndemicBreeders.checked;

  vars += "&sci_names=" + !sciNames.checked;
  vars += "&italics=" + !italics.checked;

  window.open("./php/makePDF.php" + vars + "", "_blank");
}


function uploadDownloads() {

  if (!navigator.sendBeacon) {
    console.log("sendBeacon(uploadDownloads) not supported");
    return true;
  }

  var downloadsURL = "./php/collectDownloads.php";

  var downloadData = new FormData();
  downloadData.append('country', currentCountry);
  downloadData.append('document', requested);

  navigator.sendBeacon(downloadsURL, downloadData);
}

function updateActivityData(stage, query) {

  var action = [];

  switch (stage) {

  case "start":
    action.push("start");
    break;

  case "select":
    action.push("select");
    action.push(currentCountry);
    break;

  case "search":
    action.push("search");
    if (query)  action.push(query);
    break;

  case "download":
    action.push("download");
    action.push(requested);
    break;

  case "stop":
    action.push("stop");
    break;

  default:
    break;
  }

  
}

function uploadActivity(action) {

  if (!navigator.sendBeacon) {
    return true;
  }
  var activityURL = "./php/collectActivity.php";
  

  var JSONstringData = JSON.stringify(action);

  var downloadData = new FormData();
  downloadData.append('action', JSONstringData);
  navigator.sendBeacon(activityURL, downloadData);
}

;
"use strict";


var map;

var countries = {
  "Argentina": 0, "Aruba": 1, "Bolivia": 2, "Brazil": 3, "Chile": 4,
  "Colombia": 5, "Curaçao": 6, "Ecuador": 7, "FrenchGuiana": 8,
  "Guyana": 9, "Paraguay": 10, "Peru": 11, "Suriname": 12, "Trinidad": 13,
  "Uruguay": 14, "Venezuela": 15, "Bonaire": 16, "Falklands": 17
};




var endemicColor = "#f00";
var endemicBreederColor = "url(#pattern-endemic-breeder)";

var extinctColor = "#000";
var residentColor = "#fd712f";


var nonBreederColor = "#00f";

var vagrantColor = "#0f0";
var unconfirmedColor = "#ddd";
var introducedColor = "#fee090";

var baseColor = "#8a8a7c";


var baseStrokeColor = "#eee";
var highlightStrokeColor = "#6a7377";
var darkerStrokeColor = "#333";
var selectedCountryFillColor = "#f33";






function initCurrentMap() {

  prepareSVGstyles("SAMsvg");

  map = document.getElementById("currentMap");
}



function prepareSVGstyles(obj) {

  
  

  let currentBirdMap = document.getElementById("currentBirdMap");
  

  let svgDoc;

  
  

  for (var country in countries) {

    
    var cc = currentBirdMap.querySelector("#" + country);

    if (!cc) continue;

    setSVGstyles(cc);
  }
}


function setSVGstyles(obj) {

  if (obj.nodeName === "path" || obj.nodeName === "circle") {

    obj.style.transition = "fill 1s";
  }

  else if (obj.parentNode.nodeName === "g") {

    var paths = obj.querySelectorAll("path");
    var len = paths.length;

    for (var i = 0; i < len; i++) {
      paths[i].style.transition = "fill 1s";
    }
  }
}

function fillSAMmap(skipCountry) {

  
  
  
  
  
  

  let currentBirdMap = document.getElementById("currentBirdMap");


  for (var country in countries) {

    
    var cc = currentBirdMap.querySelector("#" + country);

    if (!cc || cc === skipCountry) continue;
    newFillColor(cc, baseColor);
    newStrokeColor(cc, baseStrokeColor);
  }
  addBirdNameToMap("");
}

function newFillColor(obj, newColor) {

  if (obj.nodeName === "path" || obj.nodeName === "circle") {
    
    if (newColor === baseColor) {
      
      obj.style.fill = "#8a8a7c";
    }
    else obj.style.fill = newColor;

    if (newColor !== residentColor) obj.style.stroke = baseStrokeColor;
  }

  else if (obj.parentNode.nodeName === "g") {

    var paths = obj.querySelectorAll("path");

    var len = paths.length;
    for (var i = 0; i < len; i++) {

      if (newColor === baseColor) {
        
        paths[i].style.fill = "#8a8a7c";
      }
      else paths[i].style.fill = newColor;

      if (newColor !== residentColor) paths[i].style.stroke = baseStrokeColor;
    }
  }
}


function selectedCountryFill(selectedCountry) {

  
  
  
  
  
  

  

  let currentBirdMap = document.getElementById("currentBirdMap");
  let cc = currentBirdMap.querySelector("#" + selectedCountry);

  fillSAMmap(cc);
  newFillColor(cc, selectedCountryFillColor);
  newStrokeColor(cc, baseStrokeColor);
}

function newStrokeColor(obj, newColor) {

  
  if (obj.nodeName === "path" || obj.nodeName === "circle") {
    obj.style.stroke = newColor;
  }

  else if (obj.parentNode.nodeName === "g") {

    var paths = obj.querySelectorAll("path");

    var len = paths.length;
    for (var i = 0; i < len; i++) {

      if (obj.id === "Falklands") {
        paths[i].style.stroke = "#ddd";
      }

      else paths[i].style.stroke = newColor;
    }
  }
}



function highlightSAMmap(index, current) {

  let currentBirdMap = document.getElementById("currentBirdMap");

  
  
  

  var cList = birds[index].split("-");  

  for (var country in countries) {

    
    let cc = currentBirdMap.querySelector("#" + country);

    if (!cc) continue;

    
    if (country === "Falklands") cList[countries[country]] = cList[countries[country]].trim();

      
    switch (cList[countries[country]]) {

    case "X(e)":

      newFillColor(cc, endemicColor);
      newStrokeColor(cc, baseStrokeColor);
        break;
      
    case "X(eb)":

      newFillColor(cc, endemicBreederColor);
      newStrokeColor(cc, baseStrokeColor);
      break;

    case "X":

      newFillColor(cc, residentColor);
      
      newStrokeColor(cc, darkerStrokeColor);
      break;

    case "NB":

      newFillColor(cc, nonBreederColor);
      
      newStrokeColor(cc, darkerStrokeColor);
      break;

    case "V":

      newFillColor(cc, vagrantColor);
      newStrokeColor(cc, darkerStrokeColor);

      break;

    case "U":
      newFillColor(cc, unconfirmedColor);
      
      newStrokeColor(cc, highlightStrokeColor);
      break;

    case "IN":

      newFillColor(cc, introducedColor);
      
      newStrokeColor(cc, highlightStrokeColor);
      break;

    case "EX":

      newFillColor(cc, extinctColor);
      newStrokeColor(cc, baseStrokeColor);
      break;

    case "EX(e)":

      newFillColor(cc, extinctColor);
      newStrokeColor(cc, baseStrokeColor);
      break;

    default:
      newFillColor(cc, baseColor);
      newStrokeColor(cc, baseStrokeColor);
    }
  }

  
  
}


function alreadyInMapsCollection() {

  const len = mapsCollection.children.length;

  if (len > 0) {

    var thisInstance = map.children[0];
    
    var thisSciName = thisInstance.children[0].children[2].innerText;
    
    var birdNameCollection = mapsCollection.getElementsByClassName("smallBirdText");
    return Array.from(birdNameCollection).some(bird => bird.children[2].innerText === thisSciName);
  }

  else {
    return false;
  }
  
  
}

;



var numFamiliesList = {

		"Argentina": 88, "Aruba": 53, "Bolivia": 81, "Brazil": 95, "Chile": 69,
  "Colombia": 94, "Curaçao": 50, "Ecuador": 93, "French Guiana": 84,
  "Guyana": 83, "Paraguay": 71, "Peru": 90, "Suriname": 82, "Trinidad": 72,
  "Uruguay": 74, "Venezuela": 90, "Bonaire": 47, "Falklands": 51, "South America": 106
};

var numSpeciesList = {

		"Argentina": 1052, "Aruba": 245, "Bolivia": 1409, "Brazil": 1867, "Chile": 526,
  "Colombia": 1915, "Curaçao": 217, "Ecuador": 1683, "French Guiana": 737,
  "Guyana": 828, "Paraguay": 694, "Peru": 1905, "Suriname": 746, "Trinidad": 490,
  "Uruguay": 500, "Venezuela": 1414, "Bonaire": 209, "Falklands": 220, "South America": 3514
};






;


var results = {}; 

var html2json = {	"vagrant": "V", "unconfirmed": "U", "endemic": "X(e)",
  "extinct": "EX", "endemic-breeder": "X(eb)"
};  
  
document.addEventListener("DOMContentLoaded", function () {
  var searchInput = document.getElementById("searchInput");
  searchInput.addEventListener("input", getQuery);
});





function getQuery() {
  
  
  var badIndex = searchInput.value.search(/[^"a-zñãúáóíç'\s-]/i);

  if (badIndex !== -1) {
    searchResults.innerHTML = "<li> &nbsp; &nbsp; character not allowed</li><li></li><li></li>";
    resetSearchResultsHeight();
    if (!resultsPanelOpen) toggleSearchResultsPanel();
    return;
  }

  // trim leading whitespace
  searchInput.value = searchInput.value.trimStart();
  if (searchInput.value.length === 0) {   // if only whitespace, clear the input element
    searchInput.value = "";
  }

  // wait for at least two characters
  if (searchInput.value.length < 2) {
    return;
  }
  
  var searcFormContents = document.querySelector(".searchForm_contents");
  
  if (searchInput.value.length > 6) {
    searcFormContents.style.left = "-" + (searchInput.value.length - 6)/5 + "ch";
    searchInput.size = 20 + (searchInput.value.length - 6);
  }
  
  
  results = searchRegexTree(families, searchInput.value, countries2Postals[currentCountry], true);
  loadSearchResults(results);
}









  
















  





function getSearchSpecialsQuery(event) {
  const wrapper = event.target.closest(".searchSpecialWrapper");
  if (!wrapper || wrapper.classList.contains("notAvailable")) return;

  const special = wrapper.dataset.special;
  if (!special) return;

  lastQuery = special;
  loadSearchResults(specialSearch(families, special));
}



function specialSearch(families, special) {
  
  lastQuery = special;
  special = html2json[special];  
  
  if (special) {
    
    
    if (currentCountry === "SAM" || currentCountry === "South America") return searchExtinctOrEndemicSAM(families, special);    
    else return searchCountrySpecials(families, special, countries2Postals[currentCountry]);     
  }
}

;





function modifyQuery(term) {
  
  var searcFormContents = document.querySelector(".searchForm_contents");
  

  term = term.trimStart();
  
  if (term.length < 2) return;

  if (term.length > 6) {
    searcFormContents.style.left = "-" + (term.length - 6)/5 + "ch";
    searchInput.size = 20 + (term.length - 6);
  }
  
  
  

  
  
  

  
  
  term = term.replace(/n/g, "(n|ñ)");
  term = term.replace(/a/g, "(a|ã)");

  var regex = RegExp("[úáóíç]", "i");
  var unusedAccent = regex.test(term);
  
  if (unusedAccent) {
    term = term.replace(/ú/g, "u");
    term = term.replace(/á/g, "a");
    term = term.replace(/ó/g, "o");
    term = term.replace(/í/g, "i");
    term = term.replace(/ç/g, "c");
  }

  
  
  
  
  
  
    
  
  
    
  
  
  term = term.replace(/[ -]/g, "(-| )");   
  
  
  
  lastQuery = term;
  return term;
}




function escapeRegExp(string) {
  return string.replace(/[.*+?^@#!+=_${}()|[\]\\]/g, "\\$&"); 
}

;

 
var results = "";
  
var json2html = {	"V": "va", "IN": "intr", "U": "u", "NB": "nb", "X(e)": "endemic",
  "X(eb)": "endemic-breeder", "EX(e)": "extinct", "EX": "extinct", "X": ""  };

//  ------------------------------------------------------------------------------------------------------------  //

// eslint-disable-next-line no-unused-vars
function searchRegexTree(families, query, country, modifyBoolean) {
  

  if (modifyBoolean) query = modifyQuery(query);

  if (query) return searchAllQuery(families, query, country);     // Any country, any regex query
  else return null;
}

// ------------------------------------------------------------------------------------------------------ //

// eslint-disable-next-line no-unused-vars
function searchExtinctOrEndemicSAM(families, special) {
  
  //  country === "SAM"
  var numSpecies = 0;
  results = "";
  var familyAdded;
  
  for (var family of families) {

    familyAdded = false;
    
    for (var genus of family.genera) {
      
      var SAMbirdsExtinct_Endemic = genus.spp.filter(function(bird) {
        
        var birdOccurrenceArray = Object.values(bird);
        if (special === "EX") {
          return birdOccurrenceArray.includes("EX") || birdOccurrenceArray.includes("EX(e)");
          
        }
        
        else return birdOccurrenceArray.includes(special);  
      });
      
      SAMbirdsExtinct_Endemic.forEach(function (bird) {
        if (!familyAdded) {
          results += buildFamily({ FamilyScientific: family.Family, FamilyCommon: family.FamilyCommon });
          familyAdded = true;
        }
        results += buildBird({ index: bird.index, special: json2html[bird["SAM"]], name: bird.name, genus: genus.Genus, spp: bird.species });
        numSpecies++;
      });
    }
  }
  return { numSpecies: numSpecies, list: results };
}

































function buildFamily(f) {
  
  
  var elem = `<li data-family="${f.FamilyScientific}" class="family"><span class="fco">`;
  
  elem += f.FamilyCommon + `</span>`;
  
  elem += `<span class="fsc">${f.FamilyScientific}</span></li>`;
  return elem;
}


function buildBird(b) {
  
  
  var elem = `<li data-i="${b.index}" class="bird">`;
  
  
  if (b.special) elem += `<span class="${b.special}">`;
  
  else elem += `<span>`;
  
  
  elem += `${b.name}</span>`;
  
  elem += `<span>${b.genus} ${b.spp}</span></li>`;
  return elem;
}



function searchAllQuery(families, query, country) {
  
  var regex = RegExp(query, "i");
  
  var numFamilies = [];
  var familyAdded;
  results = "";
  var numSpecies = 0;

  families.forEach(function(family) {    // forEach is okay, there will be no `break`s
  
    familyAdded = false;
  
    if (regex.test(family.Family) || regex.test(family.FamilyCommon)) {
      results += buildFamily({ FamilyScientific: family.Family, FamilyCommon: family.FamilyCommon });
      familyAdded = true;
      numFamilies.push(family.Family);
    }
  
    family.genera.forEach(function (genus) {
    
      genus.spp.forEach(function (bird) {
      
        if (regex.test(genus.Genus.concat(" ", bird.species)) || regex.test(bird.name)) {
        
          if (!familyAdded) {
            results += buildFamily({ FamilyScientific: family.Family, FamilyCommon: family.FamilyCommon });
            familyAdded = true;
            numFamilies.push(family.Family);
          }
          results += buildBird({ index: bird.index, special: json2html[bird[country]], name: bird.name, genus: genus.Genus, spp: bird.species });
          numSpecies++;
        }
      });
    });
  });
  
  if (numFamilies.length === 1 && !numSpecies) {
    // console.log("numSpecies = " + numSpecies);
    // results = {$results, numSpecies}
    var birdsInFamily = getAllSpeciesFromOneFamily(results, numFamilies[0], country);
    results = birdsInFamily.$results;
    numSpecies = birdsInFamily.numSpecies;
  }
  // return numSpecies;
  return { numSpecies: numSpecies, list: results };
}

/* <li class="family"><span class="fco">FINCHES</span><span class="fsc">FRINGILLIDAE</span></li> */

  
  
  
  
  
  
  
  
  





function searchCountrySpecials(families, special, country) {
  
  var numSpecies = 0;
  results = "";
  var familyAdded;
  
  for (var family of families) {

    familyAdded = false;
    
    for (var genus of family.genera)  {
      
      var birdsWithSpecial = genus.spp.filter(function(bird) {    // single country, not SAM
        return (bird[country] === special);
      });

      if (birdsWithSpecial.length) {
      
        if (!familyAdded) {
          results += buildFamily({ FamilyScientific: family.Family, FamilyCommon: family.FamilyCommon });
          familyAdded = true;
        }
      
        birdsWithSpecial.forEach(function(bird) {
          results += buildBird({ index: bird.index, special: json2html[bird[country]], name: bird.name, genus: genus.Genus, spp: bird.species });
          numSpecies++;
        });
      }
    }
  }
  // return numSpecies;
  return { numSpecies: numSpecies, list: results };
}
  // <li class="family"><span class="fco">SHEARWATERS</span><span class="fsc">PROCELLARIIDAE</span></li>
  
  
  
  
  
  


function getAllSpeciesFromOneFamily($results, thisFamily, country) {
  
  var numSpecies = 0;
  results = "";
  var regex = new RegExp(thisFamily);

  for (var family of families) {  

    if (!(regex.test(family.Family))) continue;  
    
    family.genera.forEach(function (genus) {
    
      genus.spp.forEach(function (bird) {
      
        $results += buildBird({ index: bird.index, special: json2html[bird[country]], name: bird.name, genus: genus.Genus, spp: bird.species });
        numSpecies++;
      });
    });
    break;  
  }
  return { numSpecies: numSpecies, $results: $results };
}




;


function resetSearchResultsHeight() {

  searchResults.style.height = "auto";
  searchResultsScroller.style.height = "auto";

  if (searchResults.scrollHeight >= 300) {
    searchResultsScroller.style.height = "25rem";
     searchResultsScroller.style.overflowY = "auto";
  }
  else {
    
    
    
    searchResultsScroller.style.height = searchResults.scrollHeight + 2 + "px";
     searchResultsScroller.style.overflowY = "hidden";
  }

  searchResultsScroller.scrollTop = 0;
}


function resetTaxPageHeight() {

  taxPageScroller.style.height = "75vh";
  taxPageScroller.scrollTop = 0;
}


function loadSearchResults(results) {

  var term = lastQuery;

  
  
  if (lastQuery) {
    var regex = /[ãñ()|]/g;
    term = lastQuery.replace(regex, "");
  }

  if (currentCountry === "Falklands") {
    document.getElementById("searchTerm").innerHTML = "Malvinas/Falklands" + "&nbsp;&nbsp; :&nbsp;&nbsp; <span>" + term + "</span>&nbsp;&nbsp;» &nbsp;" + results.numSpecies + " species";
  }
  else {
    document.getElementById("searchTerm").innerHTML = currentCountry + "&nbsp;&nbsp; :&nbsp;&nbsp; <span>" + term + "</span>&nbsp;&nbsp;&nbsp;» &nbsp;" + results.numSpecies + " species";
  }

  if (!results.numSpecies) {

    
    searchResults.innerHTML = "<li> &nbsp; &nbsp; no matches found</li><li></li><li></li>";

    resetSearchResultsHeight();
    if (!resultsPanelOpen) toggleSearchResultsPanel();

    
    return;
  }

  document.getElementById("countrySearch").classList.remove("closed");

  searchResults.innerHTML = results.list;

  resetSearchResultsHeight();
  if (!resultsPanelOpen) toggleSearchResultsPanel();
}

;


var lastQuery;
var lastSpecies;


var families;
var numFamilies;


var birds;

var searchCountryText;


var taxPage;
var taxPanel;
var searchSpecials;
var resultsPanel;
var searchResults;
var searchResultsScroller;
var taxPageScroller;

var resultsPanelOpen = false;
var printerButton;
var closeOpenFamiliesButton;





var lastResultsSpecies;
var lastIndex;

var searchSlideUpWrapper;

var searchInstructionsOpen = true;

let taxNodeByKey = new Map();





  window.addEventListener("load", function () {

  
  

  

  printerButton = document.getElementById("printerButton");
  printerButton.addEventListener("click", printSearchResults);

  closeOpenFamiliesButton = document.getElementById("closeOpenFamiliesButton");
  closeOpenFamiliesButton.addEventListener("click", closeAllFamilies);

  searchCountryText = document.getElementById("searchCountryText");

  searchInput = document.getElementById("searchInput");
  searchSpecials = document.getElementById("searchSpecials");

  
  

  searchSpecials.addEventListener("click", getSearchSpecialsQuery);
  searchSpecials.addEventListener("keyup", getSearchSpecialsQuery);

  

  taxPage = document.getElementById("taxPage");
  taxPageScroller = document.getElementById("taxPageScroller");

  searchResults = document.getElementById("searchResults");
  searchResultsScroller = document.getElementById("searchResultsScroller");
  resultsPanel = document.getElementById("results-panel");

  searchResults.addEventListener("click", gotoMatch, false);
  taxPage.addEventListener("click", toggleFamilyOpen);
  taxPanel = document.getElementById("tax-panel");

  
  
  getTEXT("../occurrences/occurrences.txt", data => birds = data.split("\n"));

  var SAMTarget = document.getElementById( "SAM" );
  var evt = new Event("click", { "bubbles": true, "cancelable": false });
  SAMTarget.dispatchEvent( evt );
  toggleCountryMenuLayer();
});






function getTEXT(url, success) {
  var xhr = new XMLHttpRequest();
  xhr.open("GET", url);
  xhr.onreadystatechange = function () {
    if (xhr.readyState > 3 && xhr.status === 200) success(xhr.responseText);
  };
  xhr.setRequestHeader("X-Requested-With", "XMLHttpRequest");
  xhr.send();
  return xhr;
}

function getJSON(url, success) {
  var xhr = new XMLHttpRequest();
  xhr.open('GET', url, true);
  xhr.responseType = 'json';
  xhr.onload = function() {
    var status = xhr.status;
    if (status === 200) {
      success(xhr.response);
    }
    else {
      throw new Error("getJSON failed : " + xhr.response);
    }
  };
  xhr.send();
}



























function toggleFooterFocusable(disable) {

  var links = document.querySelectorAll("footer a");

  if (disable) {
    links.forEach(function (link) { link.setAttribute("tabindex", "-1"); });
  }
  else {
    links.forEach(function (link) { link.setAttribute("tabindex", "0"); });
  }
}



  
  

  
  

  
  
  

  



function loadCountryTaxonomy(country) {

  
  
  
  

  
  if (lastQuery) {
    
    document.getElementById("searchTerm").innerHTML = country + " : <span>" + lastQuery + "</span>";
  }
  else
    document.getElementById("searchTerm").innerHTML = country + " : <span>" + "enter a search term above</span>";

  if (country !== "South America") {
    searchResults.classList.remove("samTax");
    taxPage.classList.remove("samTax");
  }



  if (country === "French Guiana") {
    getJSON("JSON/FrenchGuiana/FrenchGuiana.json", getCountryJSON);
  }
  
  else if (country === "Curaçao") {
    getJSON("JSON/Curacao/Curacao.json", getCountryJSON);
  }
  else if (country === "South America") {
    getJSON("JSON/SouthAmerica/SouthAmerica.json", getCountryJSON);
    searchResults.classList.add("samTax");

    
    
    
    
    
    
    
    

    
    
    

    
    
    
    searchSpecials.querySelector('[data-special="unconfirmed"]').classList.add("notAvailable");
    searchSpecials.querySelector('[data-special="vagrant"]').classList.add("notAvailable");


    
    taxPage.classList.add("samTax");
  }
  else if (country) {
    getJSON("JSON/" + country + "/" + country + ".json", getCountryJSON);
  }



  if (country !== "South America") {
    
    
    searchSpecials.querySelector('[data-special="unconfirmed"]').classList.remove("notAvailable");
    searchSpecials.querySelector('[data-special="vagrant"]').classList.remove("notAvailable");
  }

  
  var specials = /extinct|endemic|unconfirmed|vagrant|endemic-breeder/;

  if (!lastQuery) {
    currentMap.querySelector(".saveMapButton").style.display = "none";
    document.querySelector(".colorKey").style.opacity = "0.9";
  }
  else if (specials.test(lastQuery)) {
    currentMap.querySelector(".saveMapButton").style.display = "none";
  }

  
  if (country === "South America") searchCountryText.innerHTML = "South America";
  else if (country === "Falklands") searchCountryText.innerHTML = "the Falkland Islands";
  else searchCountryText.innerHTML = country;

  if (country === "Falklands")
    document.querySelector("#treeIntroText").innerHTML = "Falklands/Malvinas" + " &nbsp; : &nbsp; " + numFamiliesList[country] + " families, " + numSpeciesList[country] + " species";
  else document.querySelector("#treeIntroText").innerHTML = country + " &nbsp; : &nbsp; " + numFamiliesList[country] + " families, " + numSpeciesList[country] + " species";
}


function toggleSearchResultsPanel() {

  resultsPanel.classList.toggle("resultsPanelBoolean");

  if (!resultsPanelOpen) {      
    
    printerButton.setAttribute("tabindex", "0");
  }
  else {                        
    
    printerButton.setAttribute("tabindex", "-1");
  }

  resultsPanelOpen = !resultsPanelOpen;
}






























































function buildTaxTree(thisCountryFamilies, country) {

  

  var occ = "";
  // TODO: "E(eb)": "eb", "U": "u"
  var json2html = {
    "V": "va", "IN": "intr", "U": "u", "NB": "nb", "X(e)": "endemic",
	   "X(eb)": "endemic-breeder", "EX(e)": "endemic extinct", "EX": "extinct", "X": ""  };

  var results = `<ul id="tree">\n\n`;

  thisCountryFamilies.forEach(function(family) {    

    
    
    

    
    results += `<li class="family" data-family="${family.Family}"><span class="fTitle"><span class="fco">${family.FamilyCommon}</span>`;
    results += `<span class="fsc">${family.Family}</span></span>\n`;
    results += `  <ul class="birds">\n\n`;

    family.genera.forEach(function (genus) {

      genus.spp.forEach(function (bird) {

        
        

        occ = bird[country];
        if (occ && occ !== "X")
          results += `  <li data-i="${bird.index}"><span class="${json2html[bird[country]]}">${bird.name}</span>`;
        else
          results += `  <li data-i="${bird.index}"><span>${bird.name}</span>`;

        results += `<span>${genus.Genus} ${bird.species}</span></li>\n`;
      });
    });
    results += `  </ul></li>\n\n`;
  });

  results += `</ul>\n`;

  taxPage.innerHTML = results;

  
  

  resetTaxPageHeight();

  indexTaxTree();
}


function indexTaxTree() {
  taxNodeByKey.clear();

  document.querySelectorAll("#tree li[data-i]").forEach(li => {
    taxNodeByKey.set(`bird:${li.dataset.i}`, li);
  });

  document.querySelectorAll("#tree li.family[data-family], #tree li.familyOpen[data-family]").forEach(li => {
    taxNodeByKey.set(`family:${li.dataset.family}`, li);
  });
}


function getCountryJSON(data) {

  families = data.birds.families;
  numFamilies = families.length;

  if (lastQuery) {
    var results = {};
    
    var specials = /extinct|endemic|unconfirmed|vagrant|endemic-breeder/;
    if (specials.test(lastQuery)) results = specialSearch(families, lastQuery);
    
    else results = searchRegexTree(families, lastQuery, countries2Postals[currentCountry]), false;

    loadSearchResults(results);
  }

  
  buildTaxTree(families, countries2Postals[currentCountry]);
}




function gotoMatch(e) {

  
  if (e.target.className === "family") navigateToFamily(e.target.lastChild.innerText);

  
  else if (e.target.className === 'fco') navigateToFamily(e.target.parentNode.lastChild.innerText);

  
  else if (e.target.className === 'fsc') navigateToFamily(e.target.innerText);


  

  if (lastSpecies && lastSpecies.classList.contains("active")) {
    lastSpecies.classList.remove("active");
  }

  
  

  var ev = e || window.event;  
  var clicked = ev.target.closest("li");  

  if (clicked) var clickedClass = clicked.className;
  else return;  

  if (clicked.className !== 'family' && clicked.className !== 'bird') return;

  
  
  

  
  

  if (lastSpecies?.className !== 'fTitle') {
    if (sciNameSpan = map.querySelector("#currentBirdName span:last-of-type")) {
      const mapSciName = sciNameSpan.innerText;
      if (mapSciName === clicked.querySelector("span:last-of-type").innerText) {
        return;
      }
    }
  }

  let familyLi = clicked;
  
  if (clicked.className === 'bird') {
    while (familyLi && !familyLi.classList.contains("family")) {
      familyLi = familyLi.previousElementSibling;
    }
  }

  const birdNode = taxNodeByKey.get(`bird:${clicked.dataset.i}`);
  const familyNode = taxNodeByKey.get(`family:${familyLi.dataset.family}`);

  var elem;

  if (clicked.className !== "family") {

    const familyUList = familyNode?.querySelector(":scope > ul.birds");
    if (!familyNode || !familyUList || !birdNode) return;

    if (!familyUList.classList.contains("open")) {
      familyUList.classList.add("open");
      familyNode.classList.remove("family");
      familyNode.classList.add("familyOpen");
    }

    
    
    
    
    
    

    const top =
      birdNode.getBoundingClientRect().top -
      taxPageScroller.getBoundingClientRect().top +
      taxPageScroller.scrollTop -
      60;     

    taxPageScroller.scroll({
      top,
      behavior: "smooth"
    });

    birdNode.classList.add("active");

    lastSpecies = birdNode;
    lastIndex = Number(clicked.dataset.i);

    if (lastResultsSpecies && lastResultsSpecies !== clicked) {
      lastResultsSpecies.classList.remove("active");
    }
    clicked.classList.add("active");
    lastResultsSpecies = clicked;  
    addBirdNameToMap(clicked);

    highlightSAMmap(lastIndex, "currentMap");

    if (mapsCollection.getElementsByClassName("smallBird").length === 5 || alreadyInMapsCollection()) {
      saveMapButton.style.display = "none";
    }
    else 
      saveMapButton.style.display = "block";
  }

  else if (clicked.className === "family") {

    if (lastResultsSpecies) {
      lastResultsSpecies.classList.remove("active");
      lastResultsSpecies = null;
    }

    const top =
      familyNode.getBoundingClientRect().top -
      taxPageScroller.getBoundingClientRect().top +
      taxPageScroller.scrollTop -
      30;

    taxPageScroller.scroll({
      top,
      behavior: "smooth"
    });
   
    lastSpecies = familyNode.firstChild;  
    lastSpecies.classList.add("active");  
  }
}

function addBirdNameToMap(name) {

  var temp = "";

  if (name) {
    temp = "<span>" + name.firstChild.textContent + "</span><br/><span>" + name.lastChild.textContent + "</span>";
    document.getElementById("currentBirdName").innerHTML = temp;
  }
  else {
    document.getElementById("currentBirdName").innerHTML = temp;
  }
}



function toggleFamilyOpen(event) {

  if (event.target.id === "taxPage") return;

  
  if (!numFamilies) return;

  event = event || window.event;  

  var familyUList;
  var familyHeader;
  var speciesTarget;
  
  if (event.target.closest("ul.birds")) speciesTarget = event.target.closest("ul.birds li");
  else {
    familyHeader = event.target.closest("li.family, li.familyOpen");
    familyUList = familyHeader.childNodes[1].nextSibling;
    
  }

  
  
  

  
  
  

  if (familyUList && !familyUList.classList.contains("open")) {  

    familyUList.classList.add("open");

    familyHeader.className = "familyOpen";

    
    

    var taxPageHeight = taxPage.getBoundingClientRect().height;

    if (familyUList.offsetTop - taxPage.scrollTop > taxPageHeight - 24) {

      

      if (familyHeader.clientHeight > taxPageHeight)
        taxPage.scrollTop = familyHeader.offsetTop;

      else
        taxPage.scrollTop += familyUList.lastElementChild.offsetTop + familyUList.lastElementChild.clientHeight;
    }
    
    var scientificFamily = familyHeader.firstChild.children[1].textContent;
    if (!scientificFamily) scientificFamily = familyHeader.firstChild.children[0].textContent;

    document.querySelector("#treeIntroText").innerHTML = currentCountry + "  &nbsp; : &nbsp; " + scientificFamily + " has " + familyUList.children.length + " species";
  }

  else if (familyUList) {    
    familyUList.classList.remove("open");
    familyHeader.className = "family";

    var reset = familyHeader.querySelectorAll(".active");

    if (reset.length) {
      reset[0].classList.remove("active");
    }
    document.querySelector("#treeIntroText").innerHTML = currentCountry + "   &nbsp; : &nbsp; " + numFamiliesList[currentCountry] + " families, " + numSpeciesList[currentCountry] + " species";
  }

  
  if (!familyUList) {

    
    

    speciesTarget.classList.add("active");

    if (speciesTarget !== lastSpecies) {

      if (lastSpecies && lastSpecies.classList.contains("active")) {
        lastSpecies.classList.remove("active");
      }
    }

    lastSpecies = speciesTarget;
    if (lastResultsSpecies) lastResultsSpecies.classList.remove("active");
    lastResultsSpecies = null;

    addBirdNameToMap(speciesTarget);

    lastIndex = Number(speciesTarget.dataset.i);
    highlightSAMmap(lastIndex, "currentMap");

    document.querySelector("#treeIntroText").innerHTML = currentCountry + "   &nbsp; : &nbsp; " + numFamiliesList[currentCountry] + " families, " + numSpeciesList[currentCountry] + " species *";

    if (mapsCollection.getElementsByClassName("smallBird").length === 5 || alreadyInMapsCollection()) {
      saveMapButton.style.display = "none";
    }
    else 
      saveMapButton.style.display = "block";
  }

  taxPage.style.zIndex = 5;
}

function closeAllFamilies() {

  for (const node of taxNodeByKey.values()) {
    if (node.matches("li.familyOpen")) {

      const familyUList = node?.querySelector(":scope > ul.birds");

      familyUList.classList.remove("open");
      familyUList.classList.add("closed");

      node.classList.add("family");
      node.classList.remove("familyOpen");
    }
  }


  
  

  

  
  

  
  

  
  document.querySelector("#treeIntroText").innerHTML = currentCountry + "   &nbsp; : " + numFamiliesList[currentCountry] + " families, " + numSpeciesList[currentCountry] + " species *";
}

function printSearchResults (evt) {

  
  var numSpecies = document.getElementsByClassName("bird").length;
  if (!numSpecies) return;

  let divId = "searchResults";

  var content = '<ul class="print-results">' + document.getElementById(divId).innerHTML + "</ul>";

  var html = '<html><head><title></title><head>';

  var css = "<style>";
  css += ".print-results { list-style-type: none; margin: 0; padding: 0 0 0 40px; }";
  css += "h3 { margin: 0 0 3ch 0; text-align: center; padding-left: 0; }";
  css += ".family, .familyOpen { margin: 2ch 0 0.5ch 10%; list-style-type: disc; }";
  css += ".birds, .bird { list-style-type: none; margin-left: 10%; padding-left: 0; }";
  css += ".fsc { position: absolute; left: 60%; }";
  css += ".bird>span { padding-left: 10px;}";
  css += ".bird span:last-child { position: absolute; left: 60%; }";
  css += "</style>";
  html += css;

  html += '</head><body>';

  
  
  var normalizedQuery = lastQuery.replace(/\(a\|ã\)/gi, "a").replace(/\(n\|ñ\)/gi, "n");

  html += '<h3>' + currentCountry + ' : &nbsp;\'' + normalizedQuery + '\'  &nbsp;&nbsp;' + numSpecies + ' species</h3>';
  html += content;
  
  html+= 	"<br><br><br>Mark Pearman, Juan Freile, Jhonathan Miranda, and Van Remsen (coordinators). Country lists. &nbsp;26&nbsp;March&nbsp;2026. A classification of the bird species of South America. American Ornithological Society. http://www.museum.lsu.edu/~Remsen/SACCCountryLists.htm";
  html += '</body></html>';

  
  

  
  
  
  

  var iframe = document.createElement("iframe");
  document.body.appendChild(iframe);

  var doc = iframe.contentDocument || iframe.contentWindow.document;
  doc.open();
  doc.write(html);
  doc.close();

  setTimeout(function () {
    iframe.contentWindow.print();
    iframe.remove();
  }, 250);

  return true;
}

;
"use strict";

var deleteMapButton;
var saveMapButton;
var mapsCollection;


var currentID = 0;



document.addEventListener("DOMContentLoaded", function () {
  saveMapButton = document.querySelector(".saveMapButton");
  saveMapButton.addEventListener("click", saveCurrentMap);
  mapsCollection = document.getElementById("mapsCollection");

  

  

  

  
  

  
  
  
  

  

  
  

  
  
  
  

});

function revealMapsCollection() {

  mapsCollection.classList.toggle("open");
  map.classList.toggle("shiftUp");
}

function deleteMap(evt)  {

  evt.stopPropagation();

  map.querySelector(".saveMapButton").style.display = "block";
  var len = mapsCollection.getElementsByClassName("smallBird").length;

  
  
  
  
  var birdInstance = this.parentNode;
  
  birdInstance.parentNode.removeChild(this.parentNode);
  map.querySelector(".saveMapButton").style.display = "block";

  repositionChildMaps(len - 1);

  
  if (mapsCollection.getElementsByClassName("smallBird").length === 5 || alreadyInMapsCollection()) {
    saveMapButton.style.display = "none";
  }
  else 
    saveMapButton.style.display = "block";
  
}

function saveCurrentMap()  {

  var len = mapsCollection.getElementsByClassName("smallBird").length;
  if (len === 5) return;

  var thisInstance = map.children[0];
  
  

  if (alreadyInMapsCollection()) return; 

  
  

  var dupNode = thisInstance.cloneNode(true);
  dupNode.classList.remove("birdMapInstance");
  dupNode.classList.add("smallBird");

  dupNode.querySelector(".birdName").classList.add("smallBirdText");

  dupNode.querySelector(".colorKey").style.display = "none";

  dupNode.querySelector(".birdName").removeAttribute("id");
  dupNode.querySelector(".smallBirdText").classList.remove("birdName");

  dupNode.querySelector(".drawing").removeAttribute("id");

  dupNode.querySelector(".drawing").firstElementChild.setAttribute("id", "thin" + currentID);

  dupNode.removeAttribute("id");

  mapsCollection.appendChild(dupNode);
  len = mapsCollection.getElementsByClassName("smallBird").length;

  if (len === 1) {
    mapsCollection.addEventListener("click", revealMapsCollection);
    mapsCollection.classList.add("namesOnlySeen");
    
    numDaysButton.classList.add("mapCollectionShowing");

    
  }
  
  map.querySelector(".saveMapButton").style.display = "none";

  repositionChildMaps(len);

  deleteMapButton = dupNode.querySelector(".deleteMapButton");
  deleteMapButton.addEventListener("click", deleteMap);

  deleteMapButton.addEventListener("mouseenter", function () {
    this.classList.add("hover");
  });

  deleteMapButton.addEventListener("mouseleave", function () {
    this.classList.remove("hover");
  });

  

  highlightSAMmap(lastIndex, "thin" + currentID);
  mapsCollection.lastChild.style.opacity = "1";
  currentID++;
}

function repositionChildMaps(numChildren) {

  var smallMaps = mapsCollection.getElementsByClassName("smallBird");

  switch (numChildren) {

  case 1:
    smallMaps[0].style.left = "45vw";
    break;

  case 2:
    smallMaps[0].style.left = "34vw";
    smallMaps[1].style.left = "41vw";
    break;

  case 3:
    smallMaps[0].style.left = "26vw";
    smallMaps[1].style.left = "30.25vw";
    smallMaps[2].style.left = "34.5vw";
    break;

  case 4:
    smallMaps[0].style.left = "21vw";
    smallMaps[1].style.left = "24vw";
    smallMaps[2].style.left = "27vw";
    smallMaps[3].style.left = "30vw";
    break;

  case 5:
    smallMaps[0].style.left = "17vw";
    smallMaps[1].style.left = "20vw";
    smallMaps[2].style.left = "23vw";
    smallMaps[3].style.left = "26vw";
    smallMaps[4].style.left = "29vw";
    break;

  default:
    break;
  }
}






  






  



  







  
